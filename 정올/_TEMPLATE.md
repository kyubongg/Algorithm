# [난이도] 문제명 - 문제번호

[문제 링크](https://jungol.co.kr/problem/문제번호)

| 항목 | 내용 |
| --- | --- |
| 푼 날짜 | YYYY-MM-DD |
| 언어 | Java |
| 결과 | 시간 000 ms / 메모리 00,000 KB |
| 분류 | <!-- 예: 구현, 시뮬레이션, BFS, 이분탐색, DP --> |

---

## 1. 문제

<!-- 원문을 그대로 붙여넣지 말고, 내가 이해한 말로 3줄 이내로 요약한다.
     "무엇이 주어지고, 무엇을 구해야 하는가"만 남기면 된다. -->

### 입력

<!-- 예: 첫 줄에 N (격자 크기), 다음 N개 줄에 각 줄마다 N개의 정수 -->

### 출력

<!-- 예: 조건을 만족하는 칸의 개수를 한 줄에 출력 -->

### 제한

<!-- 4번 복잡도 계산의 근거가 되는 숫자를 여기 적어둔다.
     예: 1 ≤ N ≤ 1,000 / 원소 값 ≤ 10^9 / 시간 제한 1초 / 테스트케이스 최대 10개 -->

### 예제

```
[입력]


[출력]

```

---

## 2. 접근

### 관찰

<!-- 문제를 읽고 알아챈 사실을 나열한다. 풀이가 아니라 '성질'을 적는다.
     예: - 값이 항상 정렬되어 있다 → 이분탐색이 가능하다
         - N이 최대 20이라 완전탐색 2^20 = 약 100만으로 충분하다 -->

### 아이디어

<!-- 관찰에서 어떻게 풀이로 넘어갔는지 한 문단.
     "그래서 ~하면 되겠다고 생각했다"까지 적는다. -->

### 알고리즘 선택

| 후보 | 가능 여부 | 채택 |
| --- | --- | --- |
| <!-- 완전탐색 --> | <!-- N=20이라 2^20으로 가능 --> | <!-- X: 더 빠른 방법이 있음 --> |
| <!-- DP --> | <!-- 부분문제가 겹침 --> | <!-- O --> |

<!-- 자료구조도 이유와 함께: 예) 앞뒤 삽입/삭제가 필요해서 ArrayList 대신 ArrayDeque -->

---

## 3. 설계

### 동작 순서

<!-- 코드를 보지 않고도 재구현할 수 있을 정도의 의사코드. 예:
1. 입력을 받아 grid[N][N]에 저장
2. 방문 배열 visited[N][N] 초기화
3. 모든 칸에 대해:
   3-1. 방문하지 않았고 값이 1이면 BFS 시작
   3-2. 큐에서 꺼내 4방향 탐색, 범위 안 + 미방문 + 값 1이면 큐에 추가
4. BFS를 시작한 횟수를 출력
-->

### 엣지 케이스

<!-- 미리 떠올린 것, 그리고 나중에 틀리고 나서 알게 된 것을 모두 적는다. 예:
- N == 1일 때
- 정답이 0인 경우 (조건을 만족하는 게 하나도 없음)
- 값이 int 범위를 넘어 long이 필요한 경우
- 입력 줄 끝 공백 / 개행 처리
-->

---

## 4. 복잡도

| 구분 | 복잡도 | 근거 |
| --- | --- | --- |
| 시간 | O(?) | <!-- 상한을 대입해서 실제 연산 횟수까지: 예) N ≤ 1,000이므로 N^2 = 10^6, 1초 제한 안에 충분 --> |
| 공간 | O(?) | <!-- 예) int[1000][1000] = 4MB, 메모리 제한 256MB 안 --> |

<!-- 왜 이 복잡도면 통과하는지 한 줄로 설명할 수 없다면 아직 설계가 덜 된 것이다. -->

---

## 5. 코드

<!-- `문제명.java`와 동일한 최종 제출 코드 -->

```java

```

---

## 6. 삽질 기록

<!-- 한 번에 맞았으면 "한 번에 통과"라고만 적고 넘어간다. -->

| 시도 | 결과 | 원인 | 고친 것 |
| --- | --- | --- | --- |
| 1 | <!-- 오답 / 시간 초과 / 런타임 에러 --> | <!-- 방문 체크를 큐에서 꺼낼 때 해서 중복 삽입 --> | <!-- 큐에 넣는 시점에 visited 표시 --> |
| 2 | 통과 | | |

---

## 7. 개선할 점

<!-- 통과했더라도 남는 아쉬움을 적는다. 예:
- Scanner 대신 BufferedReader를 썼으면 입력이 더 빨랐을 것
- 4방향 탐색을 dx/dy 배열로 빼서 중복 제거 가능
- 매번 새 배열을 만드는 대신 재사용하면 GC 부담이 줄어듦
- 더 나은 풀이: 누적합을 쓰면 O(N^2) → O(N)
-->

---

## 8. 배운 것

<!-- 다음에 비슷한 문제를 만났을 때 바로 꺼내 쓸 수 있는 한 줄 교훈 -->

### 관련 문제

<!-- - [백준 1926. 그림](https://www.acmicpc.net/problem/1926) — 같은 BFS 카운팅 패턴 -->
